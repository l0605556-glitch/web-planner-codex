interface:
  display_name: "网页版规划协作"
  short_description: "让 ChatGPT 规划复杂任务，再由 Codex 分阶段执行"
  default_prompt: "Use $web-planner-codex to plan and execute this complex project task."

policy:
  allow_implicit_invocation: true
